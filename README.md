# tah-game

An Asteroids clone. Low-res canvas (320x180, scaled up with pixelated rendering), no assets, no build step.

Play: https://thataaronguy-sunstream.github.io/tah-game/

**Controls:** `W` thrust, `A`/`D` rotate, `S` reverse thrust, `Space` fire.
